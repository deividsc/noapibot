# HTTP server (Cloud Run)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# HTTP client (inter-service calls)
httpx>=0.27.0

# Core utilities
python-dotenv>=1.0.0
websockets>=12.0

# Document processing (optional — used by agents)
pdfplumber>=0.10.0
python-docx>=1.0.0
python-pptx>=0.6.0
pandas>=2.0.0
openpyxl>=3.1.0
